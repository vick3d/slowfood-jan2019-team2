%h1 Peanuts

%header
= link_to "Sign up", new_user_registration_path 

%table
    - @categories.each do |category|
        %p= category.category
        %tr{id: dom_id(category)} 
            - category.products.each do |product|
                %tr{id: dom_id(product)} 
                    %td= product.name
                    %td= "#{product.price} SEK"
                    %td= product.description
